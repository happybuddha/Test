package com.cme.commons.validator.validators;

public abstract class CMEValidator
{
   /**
    * returns true if valid. otherwise it retruns false
    * if object is null or "" should return true, else we 
    * add required validator
    * @param o
    * @return
    */
   public abstract boolean validate(Object o);
}